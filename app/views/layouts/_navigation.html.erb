<%= link_to "Partner Portal", root_path, :class => 'brand' %>
<ul class="nav">
	<% if user_signed_in? %>
		<li>
			<%= link_to 'Cuentas', accounts_path %>
		</li>
		<li>
			<%= link_to 'Paquetes', packages_path %>
		</li>
		<li>
			<%= link_to 'Cuentas Relacionadas', related_accounts_path %>
		</li>
	<% end %>
</ul>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    <%= link_to('Cerrar Sesión', destroy_user_session_path, :method=> 'delete') %>
    </li>
    <li>
    <%= link_to('Cambiar Contraseña', edit_user_registration_path) %>
    </li>
  <% else %>
    <li>
    <%= link_to('Iniciar Sesión', new_user_session_path)  %>
    </li>
    <li>
    <%= link_to('Crear Cuenta', new_user_registration_path)  %>
    </li>
  <% end %>
  <% if user_signed_in? && current_user.admin? %>
  	<li>
  		<%= link_to 'Administrar Usuarios', users_path %>
  	</li>
  <% end %>
</ul>